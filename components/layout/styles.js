import styled from 'styled-components';

const LayoutStyles = styled.div`
  && {
    .main__content {
      min-height: 1842px;
      margin: 24px;
    }

    .site-layout {
      margin-top: 100px;
    }
  }
`;

export default LayoutStyles;